// import type { NextConfig } from "next";

// const nextConfig: NextConfig = {
//   /* config options here */
//   reactStrictMode: true,
// };

// export default nextConfig;


/** @type {import('next').NextConfig} */
const nextConfig = {
  // …paste your existing options here (images, experimental, redirects, etc.)
  // reactStrictMode: true,
};

export default nextConfig;